export interface AlarmType {
    id ? : number,
        name ? : string,
        objectType ? : number,
        category ? : number,
        level ? : number,
        description ? : string,

        objectTypeName ? : string,
}