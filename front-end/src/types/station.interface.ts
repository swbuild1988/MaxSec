export interface Station {
    id: number,
        name: string,
        position ? : string,
        managementId ? : number,
        managementName ? : string,
        icon ? : number,
        createTime ? : Date
}